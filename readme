generare migrari

dotnet ef migrations add InitialCreate

daca s-a facut o greseala
ef migrations remove

update migrari

dotnet ef database update

.\stripe.exe login
.\stripe.exe listen --forward-to localhost:5044/api/webhook

docker run -d --name my-postgres -p 5432:5432 -e POSTGRES_PASSWORD=1234 -v my-postgres-data:/var/lib/postgresql postgres:18

trebuie descarcat un program de la stripe 
trebuie rulat

.\stripe.exe login
.\stripe.exe listen --forward-to localhost:5044/api/webhook 